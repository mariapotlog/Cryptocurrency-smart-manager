import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles({
  selectEmpty: {
    margin: "auto 1.5vmin",
    minWidth: 60,
  },
  title: {
    color: "white",
    textTransform: "none"
  }
});
