
import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles(() => ({
  canvas: {
    height: "auto",
    minHeight: 50,
  },
  canva: {
    width: 150,
    maxWidth: 150,
    minHeight: 50,
    maxHeight: 50,
  }
}));